# pi-context-usage

Pi extension that adds `/context-usage` and a compact estimated context status when provider token counts are unavailable.

## Commands

- `/context-usage` -- show context-window usage with a categorized breakdown.

## Install from this monorepo

Add this package as a Pi extension package or copy `index.ts` into `~/.pi/agent/extensions/`.
