# Pi Clear Context

Adds `/clear` to reset the current context by replacing the active session with a fresh empty session.

The previous session file is linked as the parent session for history, but no prior messages are carried into the new context.
