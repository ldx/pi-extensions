# pi-handoff

Pi extension that adds `/handoff` to summarize the current session into a fresh focused session.

## Commands

- `/handoff` -- create a fresh session with a compact handoff summary.
- `/handoff <goal>` -- create a fresh session focused on a specific continuation goal.

## Notes

This package includes a local copy of the context-usage helper used to refresh the context estimate after handoff.
